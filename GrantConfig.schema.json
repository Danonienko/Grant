{
	"$schema": "https://json-schema.org/draft-07/schema#",
	"title": "Grant Bot Configuration",
	"description": "The configuration schema for Grant Discord Bot",
	"type": "object",
	"required": [
		"Schedule"
	],
	"properties": {
		"Schedule": {
			"description": "The settings for the 'Schedule' command",
			"type": "object",
			"required": [
				"ScheduleListChannel",
				"ScheduleAnnounceChannel"
			],
			"properties": {
				"ScheduleListChannel": {
					"description": "The channel where the list of a scheduled events will be posted to",
					"type": [
						"string",
						"null"
					],
					"minLength": 1
				},
				"ScheduleAnnounceChannel": {
					"description": "The channel where the start of a scheduled event will be announced to",
					"type": [
						"string",
						"null"
					],
					"minLength": 1
				}
			}
		}
	}
}